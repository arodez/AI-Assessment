# Copy this file to .env and set a real value before starting the app.
# Shared password gate for the organizer view (single shared secret, not per-user auth).
ORGANIZER_PASSWORD=changeme

# Secret used to sign the organizer session cookie. Use a long random string in real use.
SESSION_SECRET=change-this-session-secret

# Port the server listens on.
PORT=3000
